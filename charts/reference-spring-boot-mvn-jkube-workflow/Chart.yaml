apiVersion: v2
name: reference-spring-boot-mvn-jkube-workflow
description: |
  A Helm chart for Kubernetes to deploy the Ploigos Workflow shared resources needed by the
  "reference-spring-boot-mvn-jkube" application's Jenkins implementation of the workflow.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

dependencies:
- name: ploigos-workflow-jenkins-resources
  version: "~0.21.0"
  repository: "https://ploigos.github.io/ploigos-charts"
